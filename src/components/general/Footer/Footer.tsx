import React from 'react';
import classes from './Footer.module.scss';

const Footer: React.FC = () => {
  return <footer className={`${classes.container}`}>Footer here!</footer>;
};

export default Footer;
